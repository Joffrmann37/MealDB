## Design Patterns Used
MVVM was used as the prominent design pattern in concert with Combine to use reactive programming to provide UI updates.

## Architecture
Used MVVM along with clean architecture to establish a clear separation of concerns between different layers of the project(i.e. - Data(Networking), Domain(Models), and Use Cases).

## Unit Testing
Provided basic unit testing to test the retrieval of meals for the listings endpoint.
